import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'DAIoT_TP_FINAL_FRONTEND',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
